CREATE TABLE user (
    id          SERIAL PRIMARY KEY,
    name        TEXT NOT NULL,
    password    TEXT NOT NULL,
    email       TEXT NOT NULL
);

get:
SELECT * FROM user WHERE id = :id;

getAll:
SELECT * FROM user;